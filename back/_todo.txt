
WORKING


- work on frontend
    - style upload button
    - preview images, docs
    - show file size somewhere
    - check for files too large (and display error message)
    - better progress bar update (average it out)
    - access code still needed?
    - qr code of url

- Backend
    - check db for expiry
    - update download stats
    - pipe download instead of going through s3 (but cache too)
    - listen for post events manually

- get ssl certs
- add certs to node
- add mapping to router


DONE

- make sure upload still works
- test packages.json (install)
- put on git private repo
- add ssh keys to bumpy
- clone repo on bumpy
- packet (pckt.ca) ?